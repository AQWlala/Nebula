# v1.1.0 — 文档修正 / MCP Wiring / 安全修复

> **发布日期**: 2026-06-28
> **类型**: 大版本 (Minor version bump)
> **前一版本**: v1.0.0
> **变更范围**: 文档, MCP, 安全(autostart / IPC / 参数匹配)

## 变更摘要
针对 MVP 首发后的反馈进行收尾:修正文档与记忆层描述,完成 MCP wiring 接线,并修复一批安全问题(autostart panic、IPC 命令注册遗漏、前后端参数不匹配)。

## 新增功能
1. MCP wiring — 完成 MCP 客户端与命令通道接线

## 修改的功能
1. 文档修正 — README / ARCHITECTURE 记忆层描述对齐实现
2. 安全修复 — autostart panic / IPC 命令注册 / 前后端参数匹配

## 破坏性变更
(none)

## 验收标准
- [x] cargo test --lib 全通过
- [x] npm test 全通过
- [x] CI 全绿
